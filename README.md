# Sania Clothing Full Stack Next.js Project

## Project Shape

- Frontend: Next.js App Router at the repository root (`src/`)
- Backend: Express API in `apps/api/`

## Docker

For local Docker Desktop parity testing with Docker Compose, see `DOCKER.md`.

